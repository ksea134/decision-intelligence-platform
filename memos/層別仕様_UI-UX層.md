# DIP UI/UX層 仕様

> 作成日: 2026-03-19
> ステータス: Streamlit（MVP） → Cloud Run + IAP（本番: Phase 5）

---

## 1. 現在の構成

| 項目 | 内容 |
|------|------|
| フレームワーク | Streamlit |
| エントリポイント | `app.py` → `ui/components/chat.py` |
| デプロイ先 | ローカル（localhost:8501）/ Streamlit Cloud |
| 認証 | なし（URLを知っていれば誰でもアクセス可） |

### 主要ファイル

| ファイル | 役割 |
|---------|------|
| `app.py` | エントリポイント。企業選択→chat描画 |
| `ui/components/chat.py` | チャット画面全体（入力、ストリーミング表示、補足フェーズ） |
| `ui/components/sidebar.py` | サイドバー（企業選択、GCP設定） |
| `ui/components/smart_cards.py` | スマートカード定義（スタブ） |
| `ui/components/infographic.py` | インフォグラフィック描画 |

### カラー定義

| 変数名 | カラーコード | 色名 | 用途 |
|---|---|---|---|
| `_ACCENT` | `#D2FF00` | ライムイエロー（蛍光黄緑） | 見出し・強調文字 |
| `_BLUE` | `#38bdf8` | スカイブルー | 情報表示 |
| `_GREEN` | `#22c55e` | グリーン | 接続済み・成功 |
| `_RED` | `#ff4b4b` | レッド | エラー・警告 |

定義場所: `ui/components/chat.py` 134〜137行目

### 画面セクション構成

**左カラム（メイン）:**

| ラベル | 位置 | 書式 |
|--------|------|------|
| DI Dashboard | スマートカードエリアの直上 | 通常フォント、バーなし |
| DI Conversation | チャット入力欄の直上 | 通常フォント、バーなし |

**右カラム（サイドバー）:**

| ラベル | 位置 | 書式 |
|--------|------|------|
| ナビゲーション | 「はじめに」の直上 | 左右バーで挟む |
| プロンプト | 「前提知識」の直上 | 左右バーで挟む |
| 処理概要 | 「AI処理フロー」の直上 | 左右バーで挟む |
| データ取得先 | 「データソース稼働状況」の直上 | 左右バーで挟む |

## 2. 主要機能

| # | 機能 | 説明 |
|---|------|------|
| F01 | マルチ企業対応チャット | companies.csvで企業切替、履歴保持 |
| F05 | ストリーミング回答 | リアルタイムで文字が表示される |
| F06 | 思考ロジック生成 | 4ステップの思考プロセス表示 |
| F07 | インフォグラフィック生成 | JSON→HTML Exec Summary |
| F08 | 深掘り質問生成 | フォローアップ3件 |
| F09 | スマートカード | 5種のショートカットプロンプト |
| F10 | 質問履歴 | 直近5件、再実行・削除 |
| F11 | PNG保存 | html2canvas |
| F12 | PDFエクスポート | html2pdf |
| F13 | テキストコピー | クリップボード |
| F14 | テキストDL | .txt出力 |
| F15 | データソースバッジ | BQ/GCS/ローカル表示 |
| F16 | 接続エラーバナー | 警告UI |
| F17 | 稼働状況パネル | リアルタイム表示 |
| F18 | BQ実行ログパネル | SQL/件数表示 |

## 3. スマートカード仕様

### CSV管理方式（2026-03-19刷新）

スマートカードは企業ごとのCSVファイルで管理。ハードコード定義（`_smart_card_defaults.py`）は廃止済み。

**CSV形式:** `data/{企業名}/smart_cards/smart_cards.csv`
```csv
企業コード,コード,アイコン,タイトル,データソース
mazda,recent,⚡,週間ニュース,all
mazda,daily_report,📋,営業日報まとめ,gcs:営業日報
mazda,kpi,📊,KPI確認,bq
```

| 要素 | カスタマイズ | 管理場所 |
|------|-----------|---------|
| カード数 | ✅ 企業ごとに0〜25枚 | CSVの行数で決まる |
| アイコン・タイトル | ✅ 企業ごとにカスタマイズ可 | CSV |
| プロンプト（質問文） | ✅ 企業ごとにカスタマイズ可 | `data/{企業名}/smart_cards/{コード}.md` |
| データソース | ✅ カードごとに指定可 | CSV（データソース列） |

**データソース列の値:**

| 値 | Geminiに渡されるデータ |
|---|----------------------|
| `all`（またはに空欄） | 全データソース（BQ + GCS + ローカル） |
| `bq` | BigQueryのみ |
| `gcs` | GCSのみ |
| `bq+gcs` | BQとGCS |
| `gcs:キーワード` | ファイル名にキーワードを含むGCSファイルのみ |
| `bq:キーワード` | テーブル名にキーワードを含むBQテーブルのみ |

**設計思想:** Geminiに「このデータだけ使え」と指示するのではなく、そもそも不要なデータを渡さない。LLMの不確実性はコード側で吸収する（開発ルールC03）。

**ルール:**
- CSVなし → 0枚（表示なし）
- 最大25枚（26個目以降は切り捨て）
- 1行5個のグリッド表示（最大5行）

### 表示タイミング
- チャット履歴が空のとき（会話開始時）だけ表示
- 一度質問すると消える
- スマートカード経由の質問は質問履歴に追加されない

### データフロー

```
1. アプリ起動 → 企業選択
   ↓
2. data/{企業名}/smart_cards/ からプロンプト読込
   - {id}.md があれば → そのファイルの内容をプロンプトに使用
   - なければ → _smart_card_defaults.py のデフォルトプロンプトを使用
   - ファイルが空なら → 安全策としてデフォルトを使用（空プロンプトでAPIエラー防止）
   ↓
3. チャット画面に5つのボタンを表示（5列レイアウト）
   ↓
4. ユーザーがボタンをクリック
   - st.session_state["pending_prompt"] にプロンプトをセット
   - st.session_state["pending_display"] に表示ラベル（アイコン+タイトル）をセット
   - st.rerun() で画面を再描画
   ↓
5. 再描画時にpending_promptを検出 → AIにプロンプトを送信
   - is_smart_card=True フラグ付きで実行
   - 質問履歴には追加されない
   ↓
6. AIが回答を生成（通常の質問と同じフロー）
```

### 管理ファイル

| ファイル | 役割 |
|---------|------|
| `infra/_smart_card_defaults.py` | 5つのカードのID、アイコン、タイトル、説明、デフォルトプロンプト |
| `infra/file_loader.py` | `load_smart_cards()` — 企業フォルダからプロンプト上書き読込 |
| `data/{企業名}/smart_cards/{id}.md` | 企業別のカスタムプロンプト |
| `ui/components/chat.py` | `_render_smart_cards()` — ボタン描画とクリック処理 |
| `domain/models.py` | `CompanyAssets.smart_cards` — データ構造定義 |

## 4. 将来のUI（Phase 5: Cloud Run + IAP）

| 項目 | 現在（Streamlit） | 将来（Cloud Run） |
|------|-------------------|-------------------|
| フレームワーク | Streamlit | React / Next.js + FastAPI |
| 認証 | なし | IAP（Google Workspace認証） |
| ストリーミング | Streamlit内蔵 | Server-Sent Events |
| デプロイ | Streamlit Cloud | Cloud Run（Docker） |
| スケーリング | なし | オートスケーリング |

### Phase 5で併せて実施する作業
- キャッシュのStreamlit依存解消 → `memos/層別仕様_Orchestration層.md` 参照
- Memory Store統合（Redis Memorystore）
