# バグ修正履歴

---

## 2026-03-18

### #001 — BigQuery 日本語カラム名のSQL実行エラー

| 項目 | 内容 |
|------|------|
| **コミット** | f34b179 |
| **重要度** | 高（データ取得が完全に失敗する） |

### 事象
BigQueryに接続済みの状態で「売上教えて」と質問すると、「データ取得ツールに問題が発生」というエラーが表示され、データが取得できない。

### 原因
GeminiがBigQueryのSQLを生成する際、日本語カラム名（`月`、`合計`、`A事業部門` など）をバッククォート（`` ` ``）で囲まずに出力するケースがあった。

- 例: `SELECT SUM(合計) FROM ...` → BigQueryが `Illegal input character "\345"` エラーを返す
- システムプロンプトでバッククォートの使用を指示していたが、`SUM()` 等の関数内のカラム名には適用されないことがあった
- エラーメッセージ「データ取得ツールに問題が発生」はコード内に定義されたものではなく、SQLエラーを受けたGeminiが生成したテキスト

### 解決策
SQL実行前に日本語識別子を自動的にバッククォートで囲む処理を追加。

1. **`domain/sql_validator.py`** に `auto_backtick_japanese()` 関数を新設
   - SQL中の日本語を含むトークンを正規表現で検出し、`` ` `` で囲む
   - 文字列リテラル（`'...'` / `"..."`）やバッククォート内はスキップ
   - `validate_sql()` の最終ステップとして自動適用
2. **`orchestration/reasoning_engine.py`** でTool Call実行前に `validate_sql()` を呼ぶよう変更
   - バリデーションエラー時はGeminiにエラーを返して再試行させる

### 影響範囲
- BigQueryに日本語カラム名を持つテーブルが存在するすべての企業データ
- `SELECT *` のみのクエリは影響なし（カラム名を直接指定しないため）
- `SUM()`, `WHERE`, `GROUP BY` 等で日本語カラム名を使うクエリが影響を受けていた

### 修正ファイル
| ファイル | 変更内容 |
|---------|---------|
| `domain/sql_validator.py` | `auto_backtick_japanese()` 追加、`validate_sql()` に統合 |
| `orchestration/reasoning_engine.py` | SQL実行前に `validate_sql()` を呼び出し、エラー時のハンドリング追加 |

### 検証結果
| テストケース | 修正前 | 修正後 |
|-------------|--------|--------|
| `SELECT SUM(合計) FROM ...` | Illegal input character エラー | `SUM(\`合計\`)` に自動変換 → 成功 |
| `SELECT 月, A事業部門 FROM ...` | Illegal input character エラー | `\`月\`, \`A事業部門\`` に自動変換 → 成功 |
| `SELECT * FROM ...` | 成功（影響なし） | 成功（変化なし） |
| `WHERE 月 = '2025年1月'` | — | `\`月\`` に変換、文字列リテラル内はスキップ → 成功 |
| 既にバッククォート済み | 成功 | 二重付与なし → 成功 |

### 再発防止
- `validate_sql()` をSQL実行の唯一の入口として統一したため、今後のSQLもすべて自動修正対象になる
- Geminiのプロンプト指示だけに頼らず、コード側で確実にカバーする防御的設計に変更

---

### #002 — Geminiがツールを呼ばずにテキスト回答を返す（不安定動作）

| 項目 | 内容 |
|------|------|
| **コミット** | 402508b |
| **重要度** | 最高（同じ操作で結果が変わり、信頼性を損なう） |

### 事象
BigQuery接続済みで「売上教えて」と質問すると、データを取得できる場合と「データ取得ツールに問題が発生」とエラーになる場合がランダムに発生。同じ質問・同じデータなのに結果が異なり、デモや実運用で信頼できない状態。

### 原因
Gemini APIのFunction Calling（ツール呼び出し）は**確率的な判断**で行われる。システムプロンプトで「必ず`query_bigquery`を呼べ」と指示していても、Geminiがその指示を無視してテキストだけで回答するケースがあった。`temperature=0.0`でもツール呼び出しの判断には揺れがある。ログ上、全セッション中**7回**ツールが呼ばれなかったケースを確認。

### 解決策
Gemini APIの`ToolConfig`で`mode="ANY"`を指定し、**API仕様レベルでツール呼び出しを強制**した。

- **初回呼び出し**: `mode="ANY"` — Geminiは必ずFunction Callを返す（テキスト回答不可）
- **2回目以降**（データ取得後）: `mode="AUTO"` — テキスト回答やさらなるツール呼び出しを自由に判断

```python
# 初回: ツール強制
tool_config_forced = types.ToolConfig(
    function_calling_config=types.FunctionCallingConfig(mode="ANY")
)
# 2回目以降: 自動判断
tool_config_auto = types.ToolConfig(
    function_calling_config=types.FunctionCallingConfig(mode="AUTO")
)
```

### 影響範囲
- BigQuery接続時のすべての質問応答
- BQ未接続時は`tools=None`のため影響なし

### 修正ファイル
| ファイル | 変更内容 |
|---------|---------|
| `orchestration/reasoning_engine.py` | `ToolConfig(mode="ANY")`で初回ツール強制、データ取得後に`AUTO`へ切替 |

### 検証結果
| テストケース | 修正前 | 修正後 |
|-------------|--------|--------|
| 「売上教えて」×複数回 | 成功したり失敗したり（7/全セッション中で失敗） | `mode="ANY"`によりAPI仕様レベルで100%ツール発火を保証 |

### 再発防止
- プロンプト指示（自然言語）ではなく、APIパラメータ（`ToolConfig.mode`）で動作を制御
- `mode="ANY"`はGemini APIの仕様として確定的にツールを呼ぶため、確率的な揺れは発生しない
- #001（バッククォート問題）と合わせ、LLMの不確実性はすべてコード側で吸収する方針を確立

---

### #003 — mode="ANY" + ストリーミングによる応答遅延・SQL無限繰り返し

| 項目 | 内容 |
|------|------|
| **コミット** | efd289d（v2.2.0-stable） |
| **重要度** | 最高（応答が3分以上返らない、同じSQLが無限実行される） |

### 事象
`mode="ANY"`でツール強制を導入後、以下の2つの問題が発生：
1. **応答遅延**: Gemini APIの`generate_content_stream` + `mode="ANY"`の組み合わせで、応答が3分以上返らないケースが頻発
2. **SQL無限繰り返し**: ストリーム中に同じfunction_callが複数チャンクで返され、同一SQLが5〜8回繰り返し実行される

### 原因
`mode="ANY"`と`generate_content_stream`（ストリーミング）の組み合わせが不安定。
- ストリーミングでは応答がチャンク単位で返るため、function_callの境界が曖昧になる
- 同じfunction_callが複数チャンクに分散して返り、全て実行してしまう
- whileループ構造と相まって、ツール結果を返した後もGeminiが再度function_callを返し、無限ループに陥る
- `mode="ANY"`自体が悪いのではなく、**ストリーミングとの組み合わせ**が根本原因

### 解決策（v3.1 — ゼロから再設計）

**Call-1を非ストリーミングに変更し、Call-2のみストリーミングを使用する2フェーズ固定構造。**

```
Call-1: generate_content（非ストリーミング）+ mode="ANY"
  → 1回のAPI呼び出しで完結した応答を受け取る
  → function_callが1つだけ確実に含まれる
  → SQL実行は1回のみ

Call-2: generate_content_stream（ストリーミング）+ tools=None
  → ツール結果を含む履歴を渡してテキスト回答を生成
  → ストリーミングでリアルタイム表示
```

| 項目 | 旧（v1〜v2） | 新（v3.1） |
|------|-------------|-----------|
| Call-1 | ストリーミング | **非ストリーミング** |
| mode | ANY（ストリームと相性悪い） | ANY（非ストリームなら安定） |
| Call-2 | ストリーミング | ストリーミング（同じ） |
| ループ | while True | **なし（if/else固定）** |
| SQL実行回数 | 不定（繰り返しあり） | **1回固定** |
| Gemini呼び出し回数 | 不定 | **最大2回固定** |

### 影響範囲
- BigQuery接続時のすべての質問応答
- BQ未接続時はtools=Noneのため影響なし

### 修正ファイル
| ファイル | 変更内容 |
|---------|---------|
| `orchestration/reasoning_engine.py` | `stream_events`をゼロから再設計。Call-1を非ストリーミング化、whileループ廃止、2分岐固定構造に変更 |

### 再発防止 — 今後の修正で必ず守るルール
1. **`generate_content_stream` + `mode="ANY"` の組み合わせは絶対に使わない**
2. **`mode="ANY"`を使う場合は必ず`generate_content`（非ストリーミング）で使う**
3. **whileループでGemini APIを呼び出さない** — ループは構造的に無限実行のリスクがある
4. **Gemini呼び出し回数は固定値で設計する** — 「最大N回」ではなく「必ずN回」
5. **ストリーミングはテキスト回答生成（最後のCall）にのみ使用する**

---

## 2026-03-19

### #007 — ADKエージェントのinstruction累積による空応答・企業間データ混入

| 項目 | 内容 |
|------|------|
| **コミット** | （次のpushで反映） |
| **重要度** | 最高（回答が一切返らなくなる + 企業間データ混入） |

### 事象
1. 複数回質問すると回答が一切返らなくなる（空応答）
2. 企業を切り替えても前の企業のデータが回答に混入する

### 原因
`agent_definition.py`の`build_root_agent`で、グローバルなエージェントオブジェクトの`instruction`を直接追記していた（`agent.instruction = agent.instruction + company_context`）。呼び出すたびにinstructionが際限なく長くなり、最終的にトークン制限を超えて空応答になった。また、前の企業のコンテキストがリセットされずに残るため、企業間データ混入も発生。

### 解決策
各エージェントの元のinstructionを`_BASE_INSTRUCTIONS`に保存し、`build_root_agent`の呼び出しごとにベースにリセットしてから企業コンテキストを追加する方式に変更。

### 修正ファイル
| ファイル | 変更内容 |
|---------|---------|
| `orchestration/adk/agent_definition.py` | `_BASE_INSTRUCTIONS`で元のinstructionを保存、毎回リセットしてから追記 |

### 再発防止
- C05ルールを追加: グローバルオブジェクトの状態を直接変更しない。変更する場合は必ずリセット処理を入れる
- T10/T11テスト項目を追加: 繰り返し・累積テスト、企業切替往復テスト
- P01〜P04テスト実施原則を追加: 1回の確認で「直った」と判断しない

---

### #006 — 企業をまたいだ過去事例の混入（Vertex AI Search汚染データ）

| 項目 | 内容 |
|------|------|
| **重要度** | 最高（企業間のデータ分離が破れている） |

### 事象
製造業デモで「売上教えて」と質問すると、検証02の業績報告書の内容（サイバー攻撃、C事業部門のアプリ成長等）が回答に含まれる。企業間でデータが混ざっている。

### 原因
#005（ADKエンジンでGCSデータが渡されていなかった問題）の修正前に、製造業デモで質問した際、GCSの企業フィルタが正しく機能せず、検証02の報告書内容が回答に混入。その回答がVertex AI Searchに「製造業デモ」のQ&Aとして保存された。以降、製造業デモで検索するたびに汚染されたQ&Aが参照されていた。

- コードのフィルタ機能自体は正常（`company: ANY("...")` で正しくフィルタ）
- 問題は**保存されたデータ自体が汚染されていた**こと
- #005修正前のGCSデータ未注入が根本原因

### 解決策
1. Vertex AI Searchのデータストアから汚染された4件のQ&Aを手動削除
2. #005の修正により、GCSデータが企業別に正しく渡されるようになったため再発しない

### 再発防止
- #005の修正が根本対策（ADKエンジンにGCSデータを正しく渡す）
- 大きなアーキテクチャ変更後は、過去事例データの整合性も確認すること
- 汚染が疑われる場合の削除手順: `vertex_ai_search.py`の`_doc_client.delete_document`で個別削除可能

---

### #005 — ADKエンジンでGCS資料（報告書）が回答に反映されない

| 項目 | 内容 |
|------|------|
| **コミット** | （次のpushで反映） |
| **重要度** | 最高（意思決定支援AIとして報告書を無視するのは致命的） |

### 事象
数値検証02で「売上教えて」と質問しても、BigQueryの数値データだけで回答し、GCS上の業績報告書（サイバー攻撃の影響、C事業部門の成長等）の内容が一切反映されない。

### 原因
Phase 4AでADKエンジンに移行した際、`runner.py`が`build_root_agent`にGCSデータを渡していなかった。`build_root_agent`自体にもGCSデータのパラメータがなかった。

- `runner.py` 83行目: `gcs_docs`パラメータが欠落
- `agent_definition.py` 142行目: `build_root_agent`にGCS引数がない
- 結果として、ADK配下の全サブエージェント（要因分析/比較/予測/汎用）がGCS資料を一切知らない状態だった

なお、従来のReasoningEngine（非ADK）では`prompt_builder.py`経由で正しくGCSデータが注入されていたため、ADK移行前は問題なく動作していた。

### 解決策
1. `agent_definition.py`の`build_root_agent`に`gcs_docs`パラメータを追加
2. `runner.py`から`data_ctx.gcs_result.content`を渡すよう修正
3. GCS資料の注入時に「必ず回答に反映すること」「数値だけの回答は不十分」という強い指示を追加
4. 併せて、従来エンジン側（`prompt_builder.py`、`reasoning_engine.py`）のGCS指示も強化

### 修正ファイル
| ファイル | 変更内容 |
|---------|---------|
| `orchestration/adk/agent_definition.py` | `build_root_agent`に`gcs_docs`パラメータ追加、強い指示付きでサブエージェントに注入 |
| `orchestration/adk/runner.py` | `data_ctx.gcs_result.content`を`build_root_agent`に渡すよう修正 |
| `domain/prompt_builder.py` | GCS資料の注入指示を強化（従来エンジン側） |
| `orchestration/reasoning_engine.py` | BQデータ注入後にGCS資料参照の再強調を追加（従来エンジン側） |

### 再発防止
- 新しいエンジンやエージェントを追加する際は、全データソース（BQ、GCS、ローカル）が正しく渡されているかチェックリストで確認する
- GCSデータは「接続成功＋中身が空」のケースもあるため、content lengthの確認が必要

---

### #004 — logger未定義によるNameError（Streamlit Cloud）

| 項目 | 内容 |
|------|------|
| **コミット** | d6298fb |
| **重要度** | 高（Streamlit Cloudでアプリが起動しない） |

### 事象
Streamlit Cloudで企業を選択すると`NameError: name 'logger' is not defined`が発生し、アプリが動作しない。

### 原因
`ui/components/chat.py`で`logger.warning()`等を6箇所で使用していたが、`import logging`と`logger = logging.getLogger(__name__)`の定義が欠落していた。ローカル環境では別の経路で動作していたため発覚が遅れた。

### 解決策
`ui/components/chat.py`に以下を追加:
- `import logging`（import文セクション）
- `logger = logging.getLogger(__name__)`（モジュールレベル定義）

### 修正ファイル
| ファイル | 変更内容 |
|---------|---------|
| `ui/components/chat.py` | `import logging`と`logger`定義を追加 |

### 再発防止
- 新しいファイルでloggerを使用する場合は、必ず`import logging`と`logger = logging.getLogger(__name__)`をセットで記述する
- Streamlit Cloudへのpush後、必ずアプリの起動確認を行う

---
